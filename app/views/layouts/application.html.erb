<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="header-title">
        <a class="navbar-brand">
          <span>Bookers</span>
        </a>
        <div class="callapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
            <li>
             <a class="text-light"><%= link_to 'Home', books_path %></a>
            </li>
           <% if user_signed_in? %> 
            <li>
             <a class="text-light"><%= link_to 'Log out', destroy_user_session_path, method: :delete %></a>
            </li>
            <% else %>
            <li>
             <a class="text-light"><%= link_to 'About', about_path %></a>
            </li>
            <li>
             <a class="text-light"><%= link_to 'Sign up', new_user_registration_path %></a>
            </li>
            <li>
             <a class="text-light"><%= link_to 'Log in', new_user_session_path %></a>
            </li>
           <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <%= yield %>
    <div class="footer">
      <p>CopyRight Infratop.inc</p>
    </div>
  </body>
</html>
